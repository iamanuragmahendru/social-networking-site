<div class="container-fluid bg-light text-dark">
    <div class="row">
        {{>sidebar}}
        <div class="col-10">
            <div class="row">
                <div class="col">
                    <h2 class="col text-center">Search</h1>
                </div>
            </div>
            <div id="searchResults">
                <div>
                    <h3>Searched for : {{query}}</h3>
                </div>
                <div>
                    {{#each searchResults as |user|}}
                    {{user.firstName}}
                    {{user.lastName}}
                    <a href="/profile/{{user.uid}}">Goto Page</a>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</div>